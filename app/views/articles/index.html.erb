<h1>List of Articles</h1>
<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to(article.title, article) %> <%#no need for article_path(article), just article%>
    </li>
  <% end %>
</ul>

<%= link_to("New Article", new_article_path) %>